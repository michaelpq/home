#!/bin/bash
# Copy all the binary information of Postgres GIT repository into a chosen make folder
# In those repositories are also copied a couple of scripts to be used for compilation
# Some are used for html, XC, postgres, etc

#List of parameters
BASE_CODE=$HOME/git/postgres
BASE_MAKE=$HOME/make
BASE_BIN=$HOME/bin
BASE_BIN_SCRIPTS=$BASE_BIN/scripts

#First clean up everything in make folder
rm -rf $BASE_MAKE

#Copy all the necessary content into make folder
#The make itself has to be done inside make repository
cp -r $BASE_CODE $BASE_MAKE
