# Extra scripts, this is part of PATH by default
/extra

# Archives containing all the stable installations of different things
# like Postgres. Installations in those paths are not contained in PATH,
# so be sure to call stuff there with full path or dedicated scripts.
/stable
