# Ignore Postgres install folder used for development
/pgsql
# Ignore Redis install folder used for development
/redis
# Ignore archives containing all the stable installations of different
# things like Postgres. Installations in those paths are not contained
# in PATH, so be sure to call stuff there with full path or dedicated
# scripts
/stable
